<div class="rw-container rw-home">
  <section class="rw-hero">
    <h1 class="rw-hero-title">
      Plan your trip in the most effective and environmentally friendly way
    </h1>

    <p class="rw-hero-subtitle">
      Personalized itineraries + clear CO₂ impact for every travel option.
    </p>

    <div class="rw-hero-actions">
      <%= button_to "Plan your trip",
                    new_trip_path,
                    method: :get,
                    class: "rw-btn rw-btn--primary" %>
    </div>
  </section>
</div>
